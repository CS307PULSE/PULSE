class DBHandling:
    def getUserDataFromDB(spotifyUserID, field):
        #field will specify what kind of data we need from database
        return None
    def storeUserInDB(newUser):
        return None